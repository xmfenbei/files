```
    @Insert({
            "<script>",
            "insert into wifi_component (id, component_id, name, buildingid, building_floor_id, floor, locz, loc, lochash, create_time, update_time, status)",
            " values ",
            "<foreach collection='wifiComponentList' item='wifiComponent' separator=','>",
            "( #{wifiComponent.id,jdbcType=VARCHAR}, " +
                    "#{wifiComponent.componentId,jdbcType=VARCHAR}, " +
                    "#{wifiComponent.name,jdbcType=VARCHAR}, " +
                    "#{wifiComponent.buildingid,jdbcType=VARCHAR}, " +
                    "#{wifiComponent.buildingFloorId,jdbcType=VARCHAR}, " +
                    "#{wifiComponent.floor}, " +
                    "#{wifiComponent.locz}, " +
                    "Point(#{wifiComponent.locx}, #{wifiComponent.locx}), " +
                    "ST_GeoHash(#{wifiComponent.locx}, #{wifiComponent.locy}, 30), " +
                    "#{wifiComponent.createTime}, " +
                    "#{wifiComponent.updateTime}, " +
                    "#{wifiComponent.status})",
            "</foreach>",
            "</script>"
    })
```
